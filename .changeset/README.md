# Changesets

Run `pnpm changeset` for every user-visible change. Select the SemVer impact and
write a concise changelog entry. Release automation is intentionally deferred to
`REL-001`; this directory only establishes the versioning input format.
