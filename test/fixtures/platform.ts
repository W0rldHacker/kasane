export const unicodePathSegments = [
  'directory with spaces',
  'конфигурация 雪.json',
] as const;

export const crlfParserInput = 'first\r\nsecond\r\n';

export const symlinkUnavailableCodes = ['EACCES', 'EPERM', 'UNKNOWN'] as const;
